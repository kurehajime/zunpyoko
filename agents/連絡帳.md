# 連絡帳

- 2026-01-28: フェイズ1対応。ずんだもん表示用の`Stage`/`Character`を追加し、6体を横並び表示。Compositionは`zunpyoko`のみ残し、HelloWorld関連を削除。
- 2026-01-28: フェイズ2-2対応。vvprojと設定ファイルからノート時刻を算出し、`Sequence`で話している間だけ`active`クラスで強調表示するようにした。
- 2026-01-28: フェイズ2-3対応。wav再生を追加し、vvprojの最後のノート終端に合わせてCompositionの長さを動的に延長。
- 2026-01-28: アクティブ画像切替とRemotion内アニメーション化。CSSアニメ削除し、`activeMergeGapFrames`で連続ノートの間隔を設定できるようにした。
- 2026-01-28: 設定/ vvproj を `public` から動的読み込みに変更。`fetch(staticFile)`で読み込み、`calculateMetadata`で長さを動的算出。
- 2026-01-28: `public/kaerunouta/`へ素材を集約し、設定ファイル名を`config.json`に統一。画像は`public/images/`に移動。
- 2026-01-28: `public/nekohunjatta/config.json`を追加し、曲ごとの音域に合わせてnoteRangeを調整。
- 2026-01-28: `public/config_list.json`で複数configを列挙し、Rootで複数Compositionを生成。`configUrl`必須にしてリストで管理。
